# 打开网页方式

## 进入项目目录

在 PowerShell 中执行：

```powershell
cd "D:\destok\your-model-atelier(1)\your-model-atelier"
```

## 安装依赖

如果已经存在 `node_modules`，通常可以跳过这一步。首次运行或依赖缺失时执行：

```powershell
npm install
```

## 启动开发网页

```powershell
npm run dev -- --host 127.0.0.1 --port 5175
```

启动成功后，在浏览器打开：

```text
http://127.0.0.1:5175/
```

常用页面：

```text
http://127.0.0.1:5175/#/chat
http://127.0.0.1:5175/#/settings
```

## 打开生产本地网页

如果想用构建后的本地服务运行：

```powershell
npm run local
```

默认打开：

```text
http://127.0.0.1:5173/
```

## 局域网访问

如需让同一局域网内的其他设备访问，把 `.env` 中服务地址改成：

```env
SERVER_HOST=0.0.0.0
SERVER_PORT=5173
```

然后重新运行：

```powershell
npm run local
```

其他设备访问：

```text
http://你的电脑IP:5173/
```

## 常见检查

```powershell
node -v
npm -v
npm run build
```
