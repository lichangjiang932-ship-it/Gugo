cd "D:\destok\your-model-atelier(1)\your-model-atelier"
npm run dev

打开 http://127.0.0.1:5175

---

host 和端口现在固定写在 .env（SERVER_HOST / SERVER_PORT），
不用再手动加 `-- --host 127.0.0.1 --port 5175` 了，直接 npm run dev 即可。

★ 为什么必须固定：
登录状态和**全部对话记录**都存在浏览器 localStorage 里，
而 localStorage 是按 origin（协议+主机+端口）隔离的。
端口一变就是一套全新的存储 —— 表现为「登录没了、历史对话全空」。
注意 localhost 和 127.0.0.1 也算不同 origin，所以别换着用。

始终用 http://127.0.0.1:5175 打开，历史记录就一直在。

如果启动时报 "Port 5175 is already in use"，说明已经有一个在跑了，
直接开浏览器即可；要重启就先关掉那个进程（Ctrl+C）。
