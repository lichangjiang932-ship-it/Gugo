## 你的身份
你是 your-model-atelier 仓库（React + Vite + 单进程 Node + SQLite, main @ 9f08e90）的实施代理。
**工作目录**：/tmp/yma-logger（这是一个独立 git worktree，分支已经创建好叫 `feat/server-logger`，HEAD = origin/main）。
node_modules 是软链到主 worktree 的，已就绪。

## 共通硬规则（违反就重做）

1. **JSX import 必须带 `.jsx` 后缀**：`import { useT } from '../i18n/I18nProvider.jsx'`。Vite resolver 不会回退。
2. **`react-hooks/set-state-in-effect`**：仓库把它升成 error。修法是把 setState 包进 `setTimeout(() => {...}, 0)` 并返回 cleanup（HooksView/DeskView 已经这么做）。**禁止**用 `eslint-disable-line react-hooks/set-state-in-effect` 绕过。
3. **i18n 5 语对称**：任何新加 `t('foo.bar')` 必须在 `src/i18n/translations.js` 的 zh-CN/en/ja/ko/zh-TW 全部加。本任务**不允许新增 i18n key**，除非任务描述明确写出 5 个值。
4. **`uiNoPlaceholders` 测试雷区**：`src/` 下不许出现 `占位` / `二维码` / 小写英文 `placeholder`，包括注释。
5. **绝对路径**：你在 worktree 里跑，patch/Edit/Write 用绝对路径或当前 workdir 相对路径，不要跨 worktree。
6. **不要新增依赖**（npm install / package.json deps 变更），如果必须新增，**停下来在报告里写明，不要擅自添加**。
7. **提交规范**：conventional commit；分支已存在叫 `feat/server-logger`；最终自己 commit + push + 开 PR（`gh pr create --title "..." --body "..." --base main --head feat/server-logger`）。

## 质量门禁（必须全绿才能 commit）

```bash
npm run lint    # 0 error / 0 warning（main 现有 11 错，你的改动至少不能让 lint 更差；ideal: 让 lint 干净）
npm test        # ~45s, 593/593（先 `npm test 2>&1 | tail -20` 看现有 baseline，至少不能让通过数下降）
npm run build   # ~2.5s
```

测试用 node 内置 `node --test`，跑 `npm test` 大约 45s。`npm install` 不要跑（node_modules 软链好了）。

## 自检 + 自报告流程

1. 读任务体；
2. 实施；
3. 跑 lint + test + build 自检；
4. **不绿就回去修，最多 3 轮**；
5. 都绿了 → `git add -A && git -c user.email=bot@hermes -c user.name=hermes-bot commit -m "..." && git push -u origin feat/server-logger`；
6. `gh pr create --base main --head feat/server-logger --title "..." --body "..."`，把 PR URL 记下来；
7. 把最终结果写到 `/tmp/yma-logger/.task-report.md`：包含 PR URL、commit hash、lint/test/build 数字、修了/加了哪些文件、有任何 skip/降级在这里写明。

## 安全网

- 你只在这一个 worktree 里改。别 cd 出去。
- 别 `git push origin main`、别 `git merge`、别 `gh pr merge`。只 push 自己的分支 + 开 PR。
- 别动 `package.json` / `package-lock.json` / `.github/workflows/`（除非任务明确要求）。
- 别动其它 worktree（`/tmp/yma-*` 别的目录）。

---

## 你这次的具体任务

**目标**：补 AGENTS.md §2.5#7 承诺但不存在的统一 logger。

**做什么**：

1. 新建 `server/utils/logger.js`：
   - 暴露 `logger.debug/info/warn/error`
   - 行为受 `LOG_LEVEL` env 控制（默认 `info`），层级 `debug < info < warn < error`
   - debug/info 走 `console.log`，warn 走 `console.warn`，error 走 `console.error`
   - 输出格式：`[<ISO 时间>] [<level>] <msg>`
   - 必须是 ESM `export const logger = ...` 或 `export function ...`，看仓库其它 `server/utils/*.js` 风格
   - 加 `tests/utils/logger.test.js`（或者跟随仓库现有测试目录约定 — 用 `node:test` + `assert`）

2. 替换所有 server 端的 `console.log` 调用为 `logger.info`（或 debug，看语义）：
   - `server/adapters/billingAuth.js:58`
   - `server/middleware.js:123`
   - `server/lifecycle.js`（5 处）
   - `server/services/pluginRegistry.js:24`
   - `server/hub/index.js:30, 136`
   - `server/services/seedSystemSkills.js`（3 处）
   - `server/appServer.js:359`
   - 用 `grep -rn 'console\.log' server/ | grep -v node_modules` 找全
   - **不要碰** `console.warn` / `console.error`（按 AGENTS.md，它们要放行）

3. 在 `.eslintrc` / `eslint.config.js`（看仓库实际用哪个）加：
   ```js
   { rules: { 'no-console': ['error', { allow: ['warn', 'error'] }] } }
   ```
   **只对 `server/` 目录加这条规则**（用 `overrides` / `files`），不影响 `src/`。
   如果加上之后 `src/` 也红了，说明 src 也有 console.log 需要处理 — 那种情况只加到 server overrides，不动 src。

4. （注：本仓库 `server/adapters/billingAuth.js` 不存在注释掉的 console.log 泄露行，无需额外处理。）

**验收**：
- `npm run lint` 不增量回归
- `npm test` 全过
- `grep -rn 'console.log' server/ | grep -v node_modules | grep -v utils/logger.js` 应该是空
- PR 标题：`feat(server): unified logger shim + replace console.log`
